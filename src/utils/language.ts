export const language = (lang: string): string => {
  if (lang === "ru") return "Русский язык";
  else if (lang === "en") return "English language";
  else if (lang === "uz") return "O'zbek tili";
  else return "";
};
